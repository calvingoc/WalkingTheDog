<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/content_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.example.cgehredo.walkingthedog.MainActivity"
    tools:showIn="@layout/activity_main">

    <GridLayout
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:id="@+id/top_grid">
        <ImageView
            android:id="@+id/dog_picture"
            android:scaleType="centerCrop"
            android:adjustViewBounds="false"
            android:layout_gravity="top|start"/>
        <TextView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:id="@+id/mv_dog_name"
            android:layout_gravity="top|end"
            />
    </GridLayout>
    <GridLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/top_grid"
        >
        <TextView
            android:id="@+id/tv_streak"
            android:layout_gravity="start|top"
            android:text="@string/streak"
            android:layout_row="1"/>
        <TextView
            android:id="@+id/tv_steak_num"
            android:layout_gravity="start|top"
            android:layout_row="2"
            android:layout_rowWeight=".8"
            android:text="15"
            android:textSize="40sp"
            />
    </GridLayout>
</RelativeLayout>
